.box {
    padding: 30px;
    background: var(--white);
}

.box--shadowed {
    box-shadow: 0 0 6px rgba(0, 0, 0, 0.14);
    border-radius: var(--cardRadius);
}

.box__title {
    margin-bottom: 55px;
    text-align: center;
}